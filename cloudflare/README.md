# Cloudflare Infrastructure

DNS and WAF rules are managed as Terraform configuration.

## Apply changes
1. Copy `terraform.tfvars.example` to `terraform.tfvars` and fill in values
2. `terraform init`
3. `terraform plan`
4. `terraform apply`

# QuizBlitz — Mobile

Expo + React Native app for the QuizBlitz quiz platform.

> Forked from micro-mobile — the upstream project that provides the UI kit,
> auth flow, api-client, and quality gate.

## Quick start

```bash
pnpm install          # from repo root
cd apps/mobile
cp .env.example .env  # then edit
npm start
```
